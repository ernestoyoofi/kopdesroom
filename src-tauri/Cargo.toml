[package]
name = "kopdesroom-app"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "kopdesroom_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", default-features = false, features = ["tauri-runtime-wry", "wry", "config-toml", "custom-protocol"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
md-5 = "0.10"
futures = "0.3"
tiny_http = "0.12"

# Backup
#tauri = { version = "2", default-features = false, features = ["tauri-runtime-wry", "wry", "config-toml", "custom-protocol"] }
#tauri-plugin-opener = "2"
#serde = { version = "1", features = ["derive"] }
#serde_json = "1"

# tauri = { version = "2", features = [] }
# tauri-plugin-opener = "2"
# serde = { version = "1", features = ["derive"] }
# serde_json = "1"

# alloc-no-stdlib_v2 = { package = "alloc-no-stdlib", version = "=2.0.4" }
alloc-no-stdlib_v3 = { package = "alloc-no-stdlib", version = "=3.0.0" }
